<!--pages/statistics/statistics.wxml-->
<image src="../../assets/img/background/gradient.jpg" class="internal-background" />
<t-navbar class="custom-navbar-trans" left-arrow fixed="{{ false }}" bind:go-back="goBack" />
<view class="statistics">
  <view class="statistics-top">
    <view class="statistics-title">
      <text>给分统计</text>
      <t-icon name="info-circle" bind:tap="showInfoDialog" class="statistics-title-info" />
    </view>
    <view class="statistics-form">
      <picker value="{{ typeValue }}" range="{{ typeList }}" bindchange="handleTypeChange" header-text="搜索类型">
        <view class="statistics-form-picker">
          <text style="font-size: 28rpx; margin-right: 5rpx;">{{ typeList[typeValue] }}</text>
          <t-icon name="chevron-down"></t-icon>
        </view>
      </picker>
      <view>
        <input class="statistics-form-input" placeholder="输入后回车搜索" bindinput="handleInput" bindconfirm="handleConfirm" />
      </view>
    </view>
  </view>
  <view class="statistics-list" wx:if="{{ !empty }}">
    <view class="statistics-item" style="padding: 0; overflow: hidden;">
      <ad unit-id="adunit-f9f29c5e515be0c5" bindload="adLoad" binderror="adError" bindclose="adClose"></ad>
    </view>
    <view class="statistics-item" wx:for="{{ list }}" wx:key="i" bind:tap="handleDetail" data-name="{{ item.kcMc }}" data-teacher="{{ item.teacherName }}">
      <view class="item-info">
        <view class="item-title">{{ item.kcMc }}</view>
        <view class="item-teacher">授课老师：{{ item.teacherName }}</view>
      </view>
    </view>
  </view>
  <view class="internal-empty" style="flex: 1; justify-content: center;" wx:else>
    <image src="https://static.hbut.life/img/status/no-search.svg" />
    <view class="internal-empty-title">未查到相关课程</view>
    <view class="internal-empty-tip">请更换搜索内容重新查询</view>
  </view>
</view>
<t-popup visible="{{ popupVisible }}" bind:visible-change="onVisibleChange" placement="center">
  <view class="block">
    <view class="block-title">{{ popupValue.kcMc }}</view>
    <view class="block-teacher">授课老师：{{ popupValue.teacherName }}</view>
    <view class="block-detail">
      <view class="block-item">
        <view class="block-item-title">最高分</view>
        <view class="block-item-num">{{ popupValue.maxGrade }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">平均分</view>
        <view class="block-item-num">{{ popupValue.meanGrade }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">样本量</view>
        <view class="block-item-num">{{ popupValue.sampleSize }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">90分以上</view>
        <view class="block-item-num">{{ popupValue.gradesGreaterThan90 }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">80-90分</view>
        <view class="block-item-num">{{ popupValue.gradesBetween80And90 }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">70-80分</view>
        <view class="block-item-num">{{ popupValue.gradesBetween70And80 }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">60-70分</view>
        <view class="block-item-num">{{ popupValue.gradesBetween60And70 }}</view>
      </view>
      <view class="block-item">
        <view class="block-item-title">60分以下</view>
        <view class="block-item-num">{{ popupValue.gradesLessThan60 }}</view>
      </view>
    </view>
    <view class="block-tips">更新时间：{{ popupValue.updateDatetime }}</view>
    <view class="block-tips">统计数据来源于Mini湖工用户，仅显示数据样本量大于30的结果，与实际情况可能存在较大误差，仅供参考！</view>
  </view>
</t-popup>
<t-back-top theme="round" bind:to-top="onToTop"></t-back-top>